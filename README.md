# 住宅空间漫游

使用 Three.js 和 Vite 的住宅三维漫游，支持电脑和手机、房间快捷导航、门窗与家具状态切换。

**在线访问：[住宅空间漫游](https://jiangbodev.github.io/home-deco/)**

**发布状态：[GitHub Actions](https://github.com/jiangbodev/home-deco/actions/workflows/pages.yml)**

## 当前版本

- 自由穿行：允许穿过墙体、家具和户型边界，眼高固定 1.4 m；点击房间名称可返回指定位置。
- 电脑使用 WASD / 方向键移动、拖动画面转向；手机使用摇杆移动、拖动画面转向。
- 通过设置切换门窗、隐私帘、家具、吊顶等状态，支持恢复初始状态。
- 桌面和手机使用同一套模型。启动时优先加载入口及客餐厅，同时下载光照；全屋模块、光照和显卡准备完成后才结束 loading，已加载内容保留。
- 空间加载条使用清单中的真实资源大小、模型下载字节事件及贴图完成事件计算进度，共享贴图只计一次。显示下载百分比和已加载 MB；解析、材质与显卡准备阶段显示活动指示，不用定时器编造百分比。失败时显示重试。

## 模型与资源

当前入口是 `public/assets/modules/manifest.json`，不是历史单文件 `home.glb`。以下数据对应当前本地的模型细节改进版本；本轮尚未提交发布。

| 项目 | 当前值 |
| --- | ---: |
| GLB 模块 | 12 |
| 共享贴图 | 55 |
| Mesh 节点 | 1,412 |
| 三角形 | 604,161 |
| 全部模块、贴图及清单 | 约 7.14 MB |
| 加上 Cycles 光照、HDR 反射及表面资源 | 约 12.97 MB |
| 体积参考 | 20 MB，结合加载与帧率评估，不作硬上限 |

以上是资源文件字节数，不含网页 JavaScript、Draco 解码器及 HTTP 开销，不代表实际加载时长或显存占用。CI 会核验文件大小、资源完整性和烘焙版本；超过体积参考值时提示重新评估加载与帧率。

模块与贴图按内容哈希命名。基础模块保留原节点、变换与交互元数据，其他模块通过 `attachTo` 挂回原节点。禁止加载房间时重新初始化交互状态。

## Blender 工作流与性能

模型修改使用 **Blender**。2026-09-21 使用 Blender 4.5.14 LTS 将两间卧室六个床品对象从 299,990 减至 59,992 个三角形（约减少 80%），保留原材质和共享贴图。取消行走碰撞，消除主卧附近的逐三角形碰撞查询开销。

`source/home-deco-bedding.blend` 是本地从 GLB 重建的可编辑场景，保留床品减面修改器和打包贴图，按仓库规则不提交二进制 `.blend`。它不是原作者未入库的工程，不能恢复原始建模历史。重建和导出步骤见 [Blender 源文件说明](source/README.md)。不要对已减面输出反复减面；本次原始模块可从提交 `8fcd1e1` 取得。

几何修改后必须从最终模块组装烘焙源，重跑接触阴影、照明与物体接触烘焙。不得只修改元数据中的版本列表绕过失效检查。网页使用 Blender Cycles 烘焙的漫反射光照与局部 HDR 反射，并保留旧光照作为动态状态和失败回退；不是实时路径追踪。

Chrome / Apple M4 检查中，主卧同机位三角形处理量约减少 36%，绘制调用数量未减少。该比例不是 FPS 提升比例；手机及其他硬件仍需实际体验确认。参见 [性能与验证记录](review/performance-2026-09-21.md)、[光影说明](review/appearance-2026-09-19.md)。早期单文件模型、床品破洞修复和减面方案均为历史状态，详见 Git 历史。

2026-09-21 较早修订（历史数据）：在 Blender 修复洗衣区壁龛、地毯包边和玄关镜后饰面的重叠面；室外树木从 380,180 减至 135,356 个三角形（保留共享实例）。重做书本高低厚薄、横竖组合与书脊字样，调整弯枝和折面叶片；主卧书桌仅保留正面的一把椅子。整屋共 606,721 个三角形。柜体恢复模型原有米白色，减弱漆面颗粒，保留墙面的哑光区别。详见 [本轮修改与验证](review/living-2026-09-21.md)。

## 开发和校验

使用 Node.js 22，通过 HTTP 预览，不要直接打开 HTML。

```sh
npm ci
npm run dev -- --host 127.0.0.1
```

```sh
node scripts/check-loading-progress.mjs
npm run build
node scripts/check-model-budget.mjs
npm run preview -- --host 127.0.0.1
```

- `src/main.js`：渲染、自由移动、交互和加载界面。
- `src/modules.js`：模块下载、去重、预取、挂载和重试。
- `src/loading-progress.js`：空间模型与贴图的真实加载进度。
- `src/appearance.js`、`surface-finishes.js`、`object-contact.js`、`irradiance.js`、`reflections.js`：材质及离线烘焙数据接入。
- `scripts/assemble-modules.mjs`：从运行模块精确组装烘焙源。
- `scripts/verify-modules.mjs SOURCE.glb`：验证模块解码几何、索引、变换及 glTF 合法性。
- `scripts/blender-simplify-bedding.py`、`import-blender-bedding.mjs`：Blender 修改与模块接入。

全屋走查修复：在 Blender 修复玄关电视旁拱框、镜子背墙、柜门收口、浴缸及窗框的重叠面；原顶补为 60 mm 厚的封闭顶板，纠正双面材质背面光照采样。主卫快捷入口移到室内。九个房间逐一巡查，见 [走查与验证记录](review/walkthrough-2026-09-21.md)。

最新沙发调整：在 Blender 将长度从 2.20 m 改为 **2.32 m**，按用户选择改为 **三块坐垫**，布料提亮为暖白色，保留织纹、褶皱和两只装饰靠枕。全部相关光照与反射重新烘焙。见 [同机位对比](review/sofa-comparison.jpg) 和 [验证记录](review/sofa-2026-09-21.md)。

已完成的室内调整：1.4 m 视角、两张床的纯深藏青整床被、去除过时沙发 AO、提亮木柜、删除壁龛两块多余竖板、统一弧面和直面的光照；餐桌花瓶及花枝已移除，主卫内部压纹隔断默认打开、可手动开关；底部导航含主卫和次卫。窗外两棵树向外 1.8 m、向上 0.9 m。自动画质更早响应掉帧，限制绘制像素数并降低玻璃透射目标分辨率。见 [最新本地记录](review/comfort-local-2026-09-21.md) 和 [实机截图](review/comfort-details.jpg)。

## 自然光与材质拟真

Blender Cycles 对现有完整户型计算日光、窗光遮挡、五次漫反射反弹和实际餐桌灯位置的暖光。7 块地面与 509 个墙柜等表面使用离线光照图；每个表面独立降噪，避免图集串色。窗边到室内、桌下和椅脚的亮暗关系更明显，柜面仍保留米白色。

客厅、主卧分别使用同一 Blender 场景生成的局部 HDR 反射，经一次预过滤后作用于漆面、木面、台面和金属。漫游时不增加实时阴影、反射或全屏 AO 渲染。当前模型为 568,029 个三角形，模型与贴图资源约 7.16 MB；光照与模型并行加载，在 loading 内完成初始光照过渡和反射预过滤，首帧显示完整光照。

全屋模块和光照版本匹配、交互处于初始状态时才启用新光照。切换家具、门窗等状态会回退，重置后恢复，避免留下已移动物体的烘焙阴影。可选光照资源失败时使用现有光照回退，不会阻止漫游；模型加载失败则提供重试。局部反射是固定位置采样，对移动视点、镜面和状态变化存在近似。

Chrome / Apple M4、1280×900、DPR 1 的两轮固定路线检查中，前后帧间隔中位数均约 16.7 ms，新版 P95 为 16.8–17.2 ms；绘制次数、三角形数未增加。这是该电脑的局部浏览器测量，不是手机帧率或公网加载速度保证。见 [拟真改进与验证](review/realism-goal.md)。

## GitHub Pages 发布

工作流：[`.github/workflows/pages.yml`](.github/workflows/pages.yml)。

1. Pull Request 执行依赖安装、加载进度回归、生产构建和资源预算检查。
2. 合并或推送到 `main` 后，通过同样检查，再上传 `dist/` 并部署到 GitHub Pages。
3. 也可在 Actions 页面手动运行 `main` 分支的工作流。

仓库 Settings → Pages 的 Source 使用 **GitHub Actions**。站点无需登录，部署使用工作流自带 `GITHUB_TOKEN` 的 Pages/OIDC 权限，不需要额外个人令牌。检查 Actions 的 `build` 和 `deploy` 均成功后，再访问站点核对最新界面与模型；代码合并成功不等于发布已经完成。

较早的已提交走查：见 [九房间画面、交互及性能记录](review/whole-house-latest.md)。最新本地走查见 [2026-09-22 检查记录](review/whole-house-20260922.md)。

书架旁四扇通高柜门已补入 Cycles 光照，修正同色板材因漏烘焙而偏灰褐的问题：[同机位前后对比](review/cabinet-ivory-comparison.jpg)。

房间快捷切换在各房间门口落点，视线朝向室内；开放客餐厅使用通道一侧作为入口。房间识别与户型图仍使用独立的中心坐标。

初始化与切换玄关共用落点 (4.6, 1.4, 6.78)，向入户门后退约 23 cm。光照延迟下载时保持 loading，失败回退也在首帧前稳定；浏览器检查见 [启动光照验证](review/lighting-startup.json)。

电视背景墙的几块拼接正面统一乳白色和连续受光，保留转角与灯具的自然明暗：[同机位对比与说明](review/tv-wall.md)。

本轮更新：电视墙实体补齐至 2.7 m 顶部，恢复窗外树干与树枝结构并保留简化树叶，完整重新烘焙光照。该发布版本为 568,029 三角形，全部外观资源约 12.10 MB。底部不再显示餐厅；主卫、次卫、次卧切换落点后移并按真实门洞校正。[修复与验证](review/tv-tree-repair.md) · [loading 界面](review/loading-design.md)。

当前本地细节改进：电视增加真实屏幕比例、边框和玻璃层；餐桌与书桌补足圆角及木纹；客厅柜子校准木材与踢脚；次卫区分陶瓷和金属，补充排水与五金；外置洗手盆改成有坡度的圆角盆腔；厨房补充煎锅、带盖汤锅、砧板、叠碗与圆角金属水槽。门窗补充密封条、型材圆角与真实执手，压纹玻璃恢复透光。全部光照按最终模型重新烘焙。[本轮检查记录](review/detail-review.md) · [餐厅与客厅](review/detail-after.jpg) · [书桌与洗手区](review/furniture-after.jpg)。

柜体与陈列跟进：统一弧形包覆、壁龛竖边与书架的乳白饰面，踢脚改同色；沙发层板补足厚度，书本具有独立封面和书页，画框具有实际边框及靠墙支撑。已重新生成配套光照，详见 [本地修正记录](review/cabinet-display-review.md)。

设置面板仅保留画质、房门、主卫隔断、隐私帘、厨房窗、洗衣区隐藏门和重置；吊顶、家具及地板等展示选项使用既定默认状态。
