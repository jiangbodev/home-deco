# 全屋本地走查 · 2026-09-22

在 localhost:5173 的实际浏览器中逐一进入玄关、客厅、餐厅、厨房、主卧、次卧、儿童房、主卫、次卫，并转向检查主要可见表面。餐厅通过户型导航进入，底部仍为八个快捷入口。

## 画面与交互

- 柜体弧面、壁龛竖边、书架侧板与底部黑边的上一轮修正保持正常；沙发陈列层板的书本、画框结构已显示。
- 厨房能看到煎锅、带盖汤锅、砧板、叠碗和水槽；卧室深藏青被子、桌椅及门窗已检查。
- 检查路线未重新发现明显持续闪烁、天花板破洞或悬空破面。这是所查看角度的目视结论，不代表覆盖每个表面。
- 主卫镜面原为灰绿哑光占位材质，现校准为银色高金属度反射材质，并排除不适用的墙面漫反射烘焙和接触暗化。
- 门窗交互不再直接关闭静态房间反射；仍保留资源完整性与模型版本校验。主卫隔断关闭、打开均已目视复核，最终恢复默认打开。
- 镜面使用现有房间环境探针，是近似反射，尚非准确实时平面镜，不能反映动态门扇的新位置。没有增加实时反射渲染通道。

## 性能与验证

本机 M4，内置浏览器，自动画质，CSS 视口 1274×1089，设备像素比约 2.4。客厅、主卧、厨房各采样 180 个 requestAnimationFrame 间隔，同时使用短按前进和左右拖动转向。中位数均约 16.7 ms，P95 为 18.0–18.3 ms，540 帧无超过 25 ms 的间隔。该结果是短程本机采样，不能替代长时间连续移动或其他设备测试。采样在反射修正后、最终排除镜面漫反射与接触叠加前完成；最终视觉与构建另行通过。

- npm run build：通过（仍有 Three.js 大包体积提示）。
- check-model-budget：通过，全部模型与外观资源 12,973,035 字节。
- check-loading-progress：通过，含缓存、阶段切换、失败和单调进度检查。
- git diff --check：通过。
- 最终浏览器捕获的 error 日志：空。

本次仅修改运行时材质与反射处理，不增加模型、纹理或烘焙资源。保持 604,161 三角形。修改保留本地，未提交发布。
